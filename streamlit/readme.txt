$ python3.9 -m venv myenv
$ source myenv/bin/activate
$ pip install -r requirements.txt
$ streamlit run openbb_demo.py

# If you need libwebkit run the following
$ sudo apt update
$ sudo apt install libwebkit2gtk-4.0-37
